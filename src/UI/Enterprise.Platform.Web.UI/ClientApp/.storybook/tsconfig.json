{
  "_comment": "Storybook-specific TypeScript config. Extends the root project so path aliases + strictness match the app; adds stories + the Storybook config files to `include`. Referenced from `angular.json` via the `storybook` / `build-storybook` targets.",
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "outDir": "../out-tsc/storybook",
    "types": ["node"]
  },
  "include": [
    "../src/**/*.ts",
    "../src/**/*.stories.ts",
    "./*.ts"
  ],
  "exclude": [
    "../src/**/*.spec.ts",
    "../node_modules",
    "../dist"
  ]
}
