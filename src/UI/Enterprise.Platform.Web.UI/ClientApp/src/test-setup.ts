/**
 * Vitest setup — runs once per test process before any spec file.
 *
 * Placeholder for Phase 4 expansions (TestBed bootstrap, Angular testing
 * utilities, shared fixtures). Phase 2 specs don't need Angular DI, so this
 * file stays deliberately empty.
 */
export {};
