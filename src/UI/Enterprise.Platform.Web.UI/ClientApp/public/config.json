{
  "_comment": "Runtime config — consumed at boot by loadRuntimeConfig(). Post-Phase-9 the SPA is cookie-session authenticated via the BFF; no Entra IDs live here anymore. Each deployment overwrites this file with environment-specific values during release. NEVER commit secrets.",
  "apiBaseUrl": "/api/proxy/v1",
  "bffBaseUrl": "",
  "telemetry": {
    "appInsightsConnectionString": "",
    "sampleRate": 1.0,
    "webVitalsSampleRate": 0.1
  },
  "session": {
    "accessTokenLifetimeSeconds": 900,
    "warningLeadTimeSeconds": 120,
    "pollIntervalSeconds": 30
  },
  "features": {}
}
