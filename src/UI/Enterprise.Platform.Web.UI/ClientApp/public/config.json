{
  "_comment": "Runtime config — consumed at boot by loadRuntimeConfig(). NEVER commit secrets here: MSAL clientId is a public identifier; anything with entropy (keys, signing material) belongs in deployment configuration (Key Vault, pipeline variables). Each deployment overwrites this file with environment-specific values during release.",
  "apiBaseUrl": "http://localhost:5044/api/v1",
  "bffBaseUrl": "http://localhost:5001",
  "msal": {
    "clientId": "",
    "tenantId": "",
    "apiScope": "",
    "redirectUri": "http://localhost:4200",
    "postLogoutRedirectUri": "http://localhost:4200/auth/login"
  },
  "telemetry": {
    "appInsightsConnectionString": "",
    "sampleRate": 1.0
  },
  "session": {
    "accessTokenLifetimeSeconds": 900,
    "warningLeadTimeSeconds": 120,
    "pollIntervalSeconds": 30
  },
  "features": {}
}
